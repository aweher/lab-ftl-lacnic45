! client-d - AS 64604 - vive en tenant-B (172.16.100.0/24)
! No hace BGP con el fabric en este lab: alcanza con que tenga IP en el VRF
! aislado para demostrar que NO se ve con A/B/C aunque comparta hardware.
frr defaults traditional
hostname client-d
log stdout
!
end
